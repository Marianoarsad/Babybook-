{
    "installCommand": "rm -rf node_modules && npm ci",
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "cleanUrls": true,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html",
            "status": 200
        }
    ]
}
